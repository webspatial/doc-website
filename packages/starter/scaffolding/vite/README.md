# React + TypeScript + Vite + WebSpatial

This template provides a minimal setup to get React working in Vite with WebSpatial SDK.

## Setup

- `pnpm install`

## Commands

- `pnpm dev`
- `pnpm dev:spatial`: only for packaging the website into a packaged hybrid app with the built-in WebSpatial Runtime on the visionOS emulator
